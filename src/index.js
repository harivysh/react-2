import  ReactDom  from  'react-dom';

//ReactDom.render (  Hi I'm Hari ,document.getElementIdBy("don"));

import Navbar from  './Navbar';

ReactDom.render( <Navbar />, document.getElementById("don") );